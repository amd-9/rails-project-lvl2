# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

one:
  content: First comment
  creator: one
  post: one

two:
  content: Second comment
  creator: one
  post: one

nested:
  content: Comment with nested comment
  creator: one
  post: one
  ancestry: <%= ActiveRecord::FixtureSet.identify(:with_comments) %>

deep_nested:
  content: Comment with deep nested comments
  creator: one
  post: one
  ancestry: <%= [ActiveRecord::FixtureSet.identify(:with_comments), ActiveRecord::FixtureSet.identify(:nested)].join('/') %>

